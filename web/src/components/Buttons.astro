---
import "../styles/buttons.css"

const buttons = await Astro.glob("../assets/buttons/*").then(files => {
  return files.map(file => file.default.src);
});
---

<div class="buttonGrid">
{
    buttons.map(button => (
        <img src={button} class="buttonSingle"/>
    ))
}
</div>

